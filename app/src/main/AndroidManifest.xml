<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.yesia.fisikaku">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ui.HukumTiga"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".Ui.HukumDua"
            android:parentActivityName="com.yesia.fisikaku.MainActivity">

        </activity>
        <activity android:name=".Ui.HukumSatu"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".Ui.Register" />
        <activity android:name=".Ui.Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>